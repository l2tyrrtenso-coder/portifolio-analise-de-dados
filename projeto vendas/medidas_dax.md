medida faturamento total

faturamento total = sum(vendas_sujo[faturamento])

medida total de vendas

Total de vendas = COUNT(vendas_sujo[quantidade]) 

medida ticket medio

Ticket medio = DIVIDE([Faturamento total],[Total de vendas]) 

medida Quantidade vendida

Quantidade total vendida = SUM(vendas_sujo[quantidade]) 
